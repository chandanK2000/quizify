// QuizSets.js
import React from 'react';

const QuizSets = ({ subject }) => {
  // Here you can render the quiz sets for the selected subject
  return (
    <div>
      <h2>{subject}</h2>
      {/* Render quiz sets here */}
    </div>
  );
};

export default QuizSets;
